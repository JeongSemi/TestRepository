<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="barometer">
	<insert id="insert" parameterType="com.mycompany.myapp.dto.Barometer">
		<selectKey keyProperty="dataNo" resultType="int" order="AFTER"> <!-- 자동증가되는 컬럼 얻기 -->
			select barometer_datano_seq.currval from dual <!-- from dual : 실제 테이블X 그냥 구색맞추기용 -->
		</selectKey>
			insert into barometer 
			(datano, datatime, temperature, pressure)
			values (barometer_datano_seq.nextval, sysdate, #{temperature}, #{pressure}) 
	</insert>


	
</mapper>
  